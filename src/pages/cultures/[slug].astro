---
import Fiche from '../../components/Fiche.astro';
import { cultures } from '../../lib/monde';
import { contexteDe } from '../../lib/contexte';

export function getStaticPaths() {
  return cultures.map((culture) => ({ params: { slug: culture.slug }, props: { culture } }));
}

const { culture } = Astro.props;
---

<Fiche contexte={contexteDe.culture(culture)} />
