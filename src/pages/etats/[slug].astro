---
import Fiche from '../../components/Fiche.astro';
import { etats } from '../../lib/monde';
import { contexteDe } from '../../lib/contexte';

export function getStaticPaths() {
  return etats.map((etat) => ({ params: { slug: etat.slug }, props: { etat } }));
}

const { etat } = Astro.props;
---

<Fiche contexte={contexteDe.etat(etat)} />
